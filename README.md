<p align="center">
  <img src="docs/images/onepanel.svg" width="500" alt="OnePanel Logo">
</p>

<p align="center">
  OnePanel CI/CD Manager 是一个用于管理本地 Git 仓库并集成 OnePanel 进行持续集成/持续部署（CI/CD）的工具。它由高性能的 Rust 后端和现代化的 Svelte 5 前端组成。
</p>

## ✨ 当前功能 (Current Features)

- **本地仓库管理**：
  - 支持添加任意本地 Git 仓库路径进行追踪。
  - 支持从列表中移除仓库。
  - **数据持久化**: 使用 SQLite 数据库存储仓库列表和配置，确保重启后数据不丢失。
- **Git 状态监控**：
  - 实时显示当前分支 (Branch)。
  - 显示最后一次提交的信息和时间。
  - **状态检查**：直观展示仓库是 "Clean" (干净) 还是 "Dirty" (有未提交更改)。
- **Docker 管理**：
  - **镜像管理**: 查看本地镜像列表，列出标签、ID、大小和创建时间。
  - **容器管理**: 查看容器状态，支持启动、停止和管理容器。
  - **构建支持**: 支持根据仓库内的 `Dockerfile` 构建新镜像。
- **系统托盘支持**:
  - 后台静默运行，最小化到系统托盘。
  - 托盘菜单支持快速打开 Web 界面和退出应用。
- **1Panel 集成**:
  - **镜像部署**: 支持将本地构建的镜像一键上传并导入到 1Panel 服务器 (`docker save` -> 上传 -> `docker load`)。
  - **编排管理**: 查看 1Panel 上的编排文件列表，支持查看内容、更新内容以及启动/停止编排项目。
- **现代化 UI**：
  - 基于 Svelte 5 Runes API 构建。
  - 使用 Shadcn-Svelte 组件库和 Tailwind CSS v4，提供极佳的视觉体验和交互。
  - **国际化 (i18n)**: 完整支持中英文界面切换。
- **高性能后端**：
  - 使用 Rust (Axum) 提供 API 服务。
  - 集成 `git2` 库进行高效的 Git 操作。
  - 集成 `bollard` 用于 Docker 操作。
  - 集成 Scalar 自动生成交互式 API 文档。

## 🛠️ 技术栈 (Tech Stack)

- **Backend**: Rust, Axum, Tokio, git2, sqlx (SQLite), bollard (Docker), tray-icon, Utoipa (OpenAPI)
- **Frontend**: Svelte 5, Vite, Tailwind CSS v4, Shadcn-Svelte, svelte-i18n
- **Tools**: 1Panel (目标集成平台), Docker

## 🚀 快速开始 (Getting Started)

### 一键启动
```bash
cargo run
```

服务运行在 http://localhost:3000

### 后端 (Backend)
```bash
cd backend
cargo run
# 服务运行在 http://localhost:3000
# API 文档: http://localhost:3000/scalar
```

### 前端 (Frontend)
```bash
cd frontend
npm run dev
# 服务运行在 http://localhost:5173
```

